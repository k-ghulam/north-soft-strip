import { r as SERVICES } from "./site-BPt2AzFt.js";
import { createFileRoute, lazyRouteComponent, notFound } from "@tanstack/react-router";
//#region src/routes/services.$slug.tsx
var $$splitErrorComponentImporter = () => import("./services._slug-Bdg1TFoy.js");
var $$splitNotFoundComponentImporter = () => import("./services._slug-DXJ3fy4a.js");
var $$splitComponentImporter = () => import("./services._slug-boklb8pR.js");
var Route = createFileRoute("/services/$slug")({
	component: lazyRouteComponent($$splitComponentImporter, "component"),
	loader: ({ params }) => {
		const s = SERVICES.find((x) => x.slug === params.slug);
		if (!s) throw notFound();
		return s;
	},
	head: ({ loaderData }) => {
		const title = loaderData ? `${loaderData.title} Liverpool — North Soft Strip` : "Service — North Soft Strip";
		const desc = loaderData ? `${loaderData.title.toLowerCase()} in Liverpool & Merseyside. ${loaderData.short} Free quote from your local soft strip specialist.` : "";
		return {
			meta: [
				{ title },
				{
					name: "description",
					content: desc
				},
				{
					property: "og:title",
					content: title
				},
				{
					property: "og:description",
					content: desc
				},
				{
					property: "og:url",
					content: `/services/${loaderData?.slug ?? ""}`
				}
			],
			links: [{
				rel: "canonical",
				href: `/services/${loaderData?.slug ?? ""}`
			}],
			scripts: loaderData ? [{
				type: "application/ld+json",
				children: JSON.stringify({
					"@context": "https://schema.org",
					"@type": "Service",
					name: loaderData.title,
					description: loaderData.short,
					areaServed: {
						"@type": "City",
						name: "Liverpool"
					},
					provider: {
						"@type": "LocalBusiness",
						name: "North Soft Strip",
						areaServed: "Liverpool and Merseyside"
					}
				})
			}] : []
		};
	},
	notFoundComponent: lazyRouteComponent($$splitNotFoundComponentImporter, "notFoundComponent"),
	errorComponent: lazyRouteComponent($$splitErrorComponentImporter, "errorComponent")
});
//#endregion
export { Route as t };
